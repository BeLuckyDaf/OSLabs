#include <stdio.h>

typedef struct Node {
	int data;
	struct Node* next;
} Node;

// linked list will be actually just a pointer to the first node

Node* create_linked_list() {
	return (Node*)malloc();
}

int main() {
	

	return 0;
}
